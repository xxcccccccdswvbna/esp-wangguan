这四个固件版本（CT1、CT2、CT3、CT4）的**核心 BLE 控制逻辑、双核性能优化、以及满血蓝牙代理功能是完全一致的**。

它们的区别仅在于**附加功能**和**适用的硬件/网络场景**。以下是详细的对比和解析：

---

### 📊 核心功能对比一览表

| 功能模块 | CT1 (Lite) | CT2 (Full) | CT3 (Custom) | CT4 (Pro) |
| :--- | :---: | :---: | :---: | :---: |
| **基础 BLE 网关控制** (灯/风扇/传感器) | ✅ | ✅ | ✅ | ✅ |
| **双核满血性能** (无单核限制) | ✅ | ✅ | ✅ | ✅ |
| **满血蓝牙代理** (主动连接 HA) | ✅ | ✅ | ✅ | ✅ |
| **HA 自动化发 HEX 抓包指令** (`send_raw_hex`) | ✅ | ✅ | ✅ | ✅ |
| **Web 控制台 & HTTP OTA** | ✅ | ✅ | ✅ | ✅ |
| **MQTT 客户端** (接收外部 HEX 指令) | ❌ | ✅ | ❌ | ❌ |
| **物理按键支持** (单击/双击/长按/恢复出厂) | ❌ | ❌ | ❌ | ✅ (4个按键) |
| **板载 LED 状态指示** (蓝灯/白灯) | ❌ | ❌ | ❌ | ✅ (2个 LED) |
| **断网离线闪烁提示** | ❌ | ❌ | ❌ | ✅ |

---

### 📝 各版本详细解析与适用场景

#### 1. CT1 (Lite) - 基础纯净版
* **定位**：最标准、最纯净的 BLE 网关固件。
* **特点**：
  * 包含了所有核心的 BLE 设备控制、状态解析和满血蓝牙代理功能。
  * 去除了所有不必要的后台服务（如 MQTT），资源占用最小，运行最稳定。
  * 保留了 Web Server，确保可以通过网页进行本地 OTA 升级。
* **适用场景**：
  * 你使用的是普通的 ESP32 开发板（无额外按键/LED）。
  * 你的智能家居系统**完全依赖 Home Assistant (Native API)**，不需要 MQTT。
  * 追求极致的稳定性和最低的资源消耗。

#### 2. CT2 (Full) - 全能通信版
* **定位**：面向需要多协议接入和外部系统联动的用户。
* **特点**：
  * **包含 CT1 的所有功能**。
  * **额外增加了 MQTT 客户端**：会自动连接到你的 MQTT 服务器 (`192.168.6.88`)。
  * 监听专属主题（例如 `ct2/ble/send`），允许你通过 Node-RED、其他单片机或任何支持 MQTT 的工具，直接向网关发送 HEX 抓包指令。
* **适用场景**：
  * 你的智能家居系统深度依赖 MQTT（例如使用了 Node-RED 做复杂自动化，或同时接入了 Domoticz/OpenHAB）。
  * 你需要网关具备“接收外部网络指令并转化为 BLE 广播”的能力。

#### 3. CT3 (Custom) - 定制扩展版
* **定位**：一张“空白画布”，用于未来特殊需求的二次开发。
* **特点**：
  * 目前功能与 CT1 几乎完全一致。
  * 在代码架构上，它被独立出来。如果你以后想为某个特定房间（比如“车库”或“阳台”）添加特殊的传感器（如温湿度、光照）或特殊的自动化逻辑，可以直接在 Python 脚本的 CT3 生成区块中单独添加代码，而不会影响 CT1/CT2/CT4 的纯净度。
* **适用场景**：
  * 作为备用固件，或用于测试新的 ESPHome 组件/实验性功能。
  * 你有某个特定设备需要独一无二的配置，不想污染主版本。

#### 4. CT4 (Pro) - 专属硬件旗舰版
* **定位**：为定制硬件 PCB 或带有物理交互外设的 ESP32 打造的全功能旗舰。
* **特点**：
  * **包含 CT1 的所有功能**。
  * **物理按键支持**：支持 4 个物理按键 (GPIO34/35/32/33)，支持单击、双击、长按。长按 KEY1 (8秒) + 按住 KEY4 可触发**硬件级恢复出厂设置**。
  * **状态 LED**：支持蓝灯和白灯 (GPIO27/26)。当 HA 断开连接（断网）时，白灯会自动闪烁提示，联网后熄灭。
  * **开机自检**：启动时会同时点亮蓝白灯，提示系统正在初始化。
* **适用场景**：
  * 你使用的是专门设计的网关 PCB 板，上面焊了按键和 LED。
  * 你需要物理按键作为断网时的应急控制手段（例如：断网时按 KEY1 依然能开灯）。
  * 你需要直观的硬件状态指示（断网报警）。

---

### 🌟 它们的“四大共同点”（最新升级）

无论你选择哪个版本，它们现在都共享以下强大特性：
1. **双核满血运行**：移除了 `CONFIG_FREERTOS_UNICORE: y`，ESP-IDF 会将 Wi-Fi/蓝牙协议栈和业务逻辑分配到双核，大幅提升并发稳定性。
2. **满血蓝牙代理**：所有版本都默认开启 `bluetooth_proxy: active: true`，都能作为 HA 的强大蓝牙中枢。
3. **支持 HA 自动化发抓包指令**：所有版本都注册了 `send_raw_hex` 服务，你可以在 HA 自动化中直接调用它发送 HEX 数据。
4. **动态命名防冲突**：每个版本的内部 Gateway ID 都是动态的（如 `ct1_ble`, `ct2_ble`），即使你把多个固件刷入不同的设备，它们也不会互相干扰。

---

### 💡 选型建议：我该刷哪个？

* **如果你用的是市售的普通 ESP32 开发板（如 ESP32-DevKitC）**：
  * 纯 HA 用户 👉 刷 **CT1**
  * 需要用 MQTT / Node-RED 👉 刷 **CT2**
* **如果你用的是自己画的 PCB，且板子上有按键和 LED**：
  * 毫不犹豫，刷 **CT4**，享受完整的物理交互体验。
* **如果你只是想留一个版本以后自己瞎折腾改代码**：
  * 用 **CT3**。

你可以根据你手头硬件的实际情况，在 Python 脚本的 `PROJECT_PREFIX` 中统一修改名字，然后一次性编译出这四个版本，按需刷入不同的设备！
